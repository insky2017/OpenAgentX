version: 1
agent_id: orchestrator
transport: unix
unix_socket: run/openagentx.sock
capabilities:
  - understand
  - decompose
  - delegate
  - review
runtime_backends:
  - backend_id: primary
    adapter_id: acp
    options:
      provider: codex
