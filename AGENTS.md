# OpenAgentX Agent Guide

本文件是 OpenAgentX 子项目的实施、测试和审计规则。冻结的 ADR、测试计划和本文件共同构成执行基线；任何实现不得以测试暂时变绿为理由绕过领域边界。

## 任务执行规则

1. **先定义验收矩阵，再写代码**：每个任务开始前必须列出正向流程、状态不变量、CAS/幂等约束、失败路径、竞态场景和证据格式。没有明确证据的“通过”不得成立。
2. **先审边界，后做集成测试**：编码前检查任务依赖的 API、数据库事务、外部 CLI/协议、身份和部署条件；集成测试用于确认实现，不用于替代架构设计。
3. **严格控制任务范围**：P0/P1 问题阻塞当前任务；P2 问题记录到缺陷台账并归属后续任务，除非它直接破坏当前验收条件。不得在测试过程中无限扩大任务目标。
4. **实现、验证、审计、提交闭环**：一个任务只允许一个主要实现批次和一次独立验证批次。验证失败时集中修复并重跑当前任务；通过后立即提交独立 Git commit，再进入下一任务。
5. **代理分工不得重复消耗**：实现代理负责代码，验证代理负责批量测试和证据，主代理负责边界审查、结果判定和提交。不得让多个代理重复执行相同审计或相同长时间测试。
6. **长任务按分钟等待**：对真实 Runtime、网络、浏览器和部署操作使用足够长的等待窗口；只有在预定检查点或出现明确错误时查询，不做高频轮询。

## 外部 Runtime 与部署规则

7. **先核对外部契约**：接入 Agent CLI 或协议前，必须记录实际版本及 help/协议中对 argv、stdin、stdout、stderr、退出码、超时、模型、推理强度、权限和 session 的承诺；配置声明不得超过实测能力。
8. **包装器和环境显式验证**：CLI wrapper（例如 `agy-graft`）、代理环境、工作目录和模型选择必须作为测试输入的一部分验证；不能用另一个命令、默认环境或未记录的 shell 状态替代正式运行路径。
9. **真实执行必须验证副作用**：Runtime 返回零退出码或 Task 进入终态不等于业务成功。必须核对可验证的终态记录、结果内容、Event Journal、RunAttempt 和实际 workspace/工具结果；无法确认副作用时保持 `uncertain`，不得自动重试为成功。
10. **解析器必须 fail closed**：空输出、畸形输出、缺少终态、诊断信息只出现在 stderr、退出码与输出语义不一致等情况必须保留脱敏诊断并拒绝推断成功。
11. **测试产物必须可追溯**：部署或重启服务时，记录源码 commit、二进制 SHA-256、配置版本、schema 版本、Worker instance/generation/fencing 和服务状态；运行中的二进制必须就是已验证的构建产物。
12. **资源与生命周期也要验收**：除任务结果外，必须验证 Worker 在空闲时持续等待、不 busy-loop，完成一个 Task 后仍在线，并能在无 tmux/pane/人工注入的情况下领取下一项工作。

## 状态、安全与证据规则

13. **权威状态走正式入口**：测试和工具优先使用公开 Control/Worker API；不得通过直接改 SQLite、伪造内部对象或人工修改状态绕过事务、RBAC、lease、fencing 和审计逻辑。隔离单元测试可使用专用 fixture，但不能冒充端到端证据。
14. **事务边界必须可证明**：复合状态变更要测试提交和故障回滚；状态表、Mailbox、RunAttempt、SessionBinding 和 Event Journal 的成败关系必须有明确断言。
15. **组合故障要验证优先级**：错误 token、principal、generation、lease、fencing、资源归属同时出现时，必须验证服务返回最先应生效的安全错误，而不是被后续 ownership 检查掩盖。
16. **敏感信息不得进入证据**：密码、Session Token、Cookie、CSRF、mTLS 私钥、完整 fencing token、代理凭据和未脱敏 Runtime 输出不得写入日志、报告、命令参数或 Git。代理设置只通过受控环境加载。
17. **生产验收使用隔离且可逆输入**：正式入口只执行低副作用、可检查、可回收的任务；破坏性测试使用隔离数据库、端口、证书和 Worker。服务修改后必须重启对应服务并确认状态。
18. **前端必须真实验证**：涉及指挥台、PWA 或移动布局时，必须用真实浏览器检查 DOM、渲染、触控、SSE/重连、认证和离线写保护；静态检查不能替代浏览器证据。

## 关卡与 ADR 规则

19. 每个测试关卡报告必须明确测试边界、未覆盖项、失败证据、修复 commit 和剩余风险；不能把 preflight、单元测试或推测写成真实 E2E 通过。
20. 每关提交前检查冻结 ADR 未被修改（包括哈希或等价 diff 校验）、工作树变更可解释、测试计划状态与报告一致。发现前置关卡的缺陷时，先记录影响和修复范围，再决定是否回开关卡，不得静默覆盖历史结论。

